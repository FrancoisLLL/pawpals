const typeFilterForm = document.getElementById("type-filter-form")

const inputs = typeFilterForm.querySelectorAll("option")
console.log(inputs)

// filterForm.onchange = handleChange;

// function handleChange(event) {

// }